#!/usr/bin/env bash

BUILD_DIR=$1
CACHE_DIR=$2

WOLFPACK_SSH_KEY="${BUILD_DIR}/.ssh/heroku_wolfpack_id_rsa"

mkdir -p $(dirname ${WOLFPACK_SSH_KEY})
rm -f ${WOLFPACK_SSH_KEY}
ssh-keygen -t rsa -N "" -f ${WOLFPACK_SSH_KEY}
